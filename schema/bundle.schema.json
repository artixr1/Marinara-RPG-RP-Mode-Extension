{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Kenhito/Marinara-RPG-RP-Mode-Extension/schema/bundle.schema.json",
  "title": "Marinara-RPG-RP-Mode-Extension Ruleset Bundle",
  "description": "A single-file install bundle that wraps a ruleset, GM agent prompt, and lorebook in one envelope. Pasted into the extension's ruleset dialog to auto-install all three pieces via Marinara's API.",
  "type": "object",
  "required": [
    "schema",
    "version",
    "ruleset",
    "lorebook"
  ],
  "properties": {
    "schema": {
      "const": "mrrp-bundle",
      "description": "Discriminator the extension uses to route bundle vs plain ruleset paste."
    },
    "version": {
      "type": "integer",
      "const": 1,
      "description": "Bundle envelope version. Bump for breaking schema changes; readers refuse unknown versions."
    },
    "minExtensionVersion": {
      "type": "string",
      "description": "Optional. If set, the framework refuses to install when its own version is lower (e.g., \"0.3.0\")."
    },
    "authorId": {
      "type": "string",
      "description": "Optional namespace for distinguishing same-id rulesets from different authors. Defaults to \"local\". Used in idempotency keys.",
      "default": "local"
    },
    "generator": {
      "type": "object",
      "description": "Optional metadata about what tool produced the bundle (helpful for triage).",
      "properties": {
        "name": {
          "type": "string"
        },
        "version": {
          "type": "string"
        }
      }
    },
    "ruleset": {
      "type": "object",
      "description": "Full ruleset.json content. Validated against the ruleset schema separately.",
      "required": [
        "id",
        "name",
        "version",
        "dice",
        "resolution",
        "attributes",
        "skills"
      ]
    },
    "gmAgent": {
      "type": "object",
      "description": "Custom Marinara agent that injects ruleset prose into pre-generation context.",
      "required": [
        "name",
        "promptTemplate"
      ],
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1,
          "maxLength": 200,
          "description": "Display name. Will be prefixed with \"MRRP: \" by the installer for grouping."
        },
        "description": {
          "type": "string"
        },
        "phase": {
          "type": "string",
          "enum": [
            "pre_generation",
            "parallel",
            "post_processing"
          ],
          "default": "pre_generation"
        },
        "promptTemplate": {
          "type": "string",
          "minLength": 50,
          "description": "Prose handed to the LLM as system context before each turn. Will be prefixed with [mrrp-v1:<authorId>/<rulesetId>] for idempotent re-install."
        },
        "settings": {
          "type": "object",
          "description": "Optional agent settings. The installer sets mrrpManaged: true and mrrpBundleSchema: \"mrrp-bundle\" automatically."
        }
      }
    },
    "additionalAgents": {
      "type": "array",
      "description": "Optional additional pre_generation context_injection agents installed alongside the main gmAgent. Each provides a focused capability (state-reminder, combat-adjudicator, lore-query, npc-bookkeeper, etc). Idempotency: each agent's settings carry mrrpAgentRole=<role> so re-install matches by (rulesetId, role).",
      "items": {
        "type": "object",
        "required": [
          "role",
          "name",
          "promptTemplate"
        ],
        "properties": {
          "role": {
            "type": "string",
            "minLength": 1,
            "maxLength": 50,
            "pattern": "^[a-z][a-z0-9-]*$",
            "description": "Stable identifier for this agent within the bundle (e.g. \"state-reminder\", \"combat-adjudicator\"). Used in idempotency keys."
          },
          "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 200,
            "description": "Display name. Prefixed with \"MRRP: \" by the installer."
          },
          "description": {
            "type": "string"
          },
          "phase": {
            "type": "string",
            "enum": [
              "pre_generation",
              "parallel",
              "post_processing"
            ],
            "default": "pre_generation"
          },
          "enabled": {
            "type": "boolean",
            "default": false,
            "description": "If true, the installer creates this sub-agent enabled. If false or absent, the agent is created DISABLED \u2014 user opts in via Marinara Settings \u2192 Agents. Sub-agents fire on every turn while enabled, so disabled-by-default keeps installs lean."
          },
          "promptTemplate": {
            "type": "string",
            "minLength": 50,
            "description": "Prose handed to the LLM as system context. Prefixed with [mrrp-v1:<authorId>/<rulesetId>:<role>] for idempotent re-install."
          },
          "settings": {
            "type": "object",
            "description": "Optional agent settings. The installer sets mrrpManaged, mrrpBundleSchema, mrrpRulesetId, mrrpAuthorId, mrrpAgentRole automatically."
          }
        }
      }
    },
    "regexScripts": {
      "type": "array",
      "description": "Optional engine-native RegexScript entries (Vector 9 of the RP→GM port feasibility doc). Each script rewrites engine-emitted surface text in AI output so the user sees ruleset-flavored prose instead of d20-default vocabulary. Bundle ships everything except server-assigned id/createdAt/updatedAt. Idempotency: installer wipes scripts whose name starts with `MRRP: <rulesetId>:` then re-POSTs. Author override > derivation — if ruleset.json ships its own regexScripts block, build-regex-scripts.mjs passes it through verbatim.",
      "items": {
        "type": "object",
        "required": [
          "name",
          "findRegex",
          "replaceString",
          "placement"
        ],
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 200,
            "description": "Display name. Prefixed with `MRRP: <rulesetId>: ` by the installer for idempotency."
          },
          "enabled": {
            "type": "boolean",
            "default": true
          },
          "findRegex": {
            "type": "string",
            "minLength": 1,
            "description": "Pattern WITHOUT delimiters (engine wraps it as new RegExp(findRegex, flags))."
          },
          "replaceString": {
            "type": "string",
            "description": "Replacement; supports $1, $2 backreferences."
          },
          "trimStrings": {
            "type": "array",
            "items": { "type": "string" },
            "default": []
          },
          "placement": {
            "type": "array",
            "items": {
              "type": "string",
              "enum": [ "ai_output", "user_input" ]
            },
            "minItems": 1
          },
          "flags": {
            "type": "string",
            "default": "gi"
          },
          "promptOnly": {
            "type": "boolean",
            "default": false
          },
          "order": {
            "type": "integer",
            "default": 100
          },
          "minDepth": {
            "type": [ "integer", "null" ],
            "default": null
          },
          "maxDepth": {
            "type": [ "integer", "null" ],
            "default": null
          }
        }
      }
    },
    "customTools": {
      "type": "array",
      "description": "Optional engine-native CustomTool entries (Vector 3 of the RP→GM port feasibility doc). Static reference tools the AI can call mid-generation for authoritative ruleset detail. Bundle ships everything except server-assigned id/createdAt/updatedAt. Idempotency: installer wipes tools by name prefix `mrrp_<rulesetIdSnake>_` then re-POSTs. Author override > derivation. SCRIPT-type tools (math via scriptBody) are NOT shipped in v0.2.x bundles — they require CUSTOM_TOOL_SCRIPT_ENABLED on the engine and are reserved for Vector 3.1.",
      "items": {
        "type": "object",
        "required": [
          "name",
          "description",
          "executionType"
        ],
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100,
            "pattern": "^[a-z][a-z0-9_]*$",
            "description": "Lowercase snake_case tool name. Engine enforces ^[a-z][a-z0-9_]*$ regex. Installer prefixes with `mrrp_<rulesetIdSnake>_` for idempotency."
          },
          "description": {
            "type": "string",
            "minLength": 1,
            "maxLength": 500
          },
          "parametersSchema": {
            "type": "object",
            "default": {}
          },
          "executionType": {
            "type": "string",
            "enum": [ "webhook", "static", "script" ],
            "default": "static"
          },
          "webhookUrl": {
            "type": [ "string", "null" ],
            "default": null
          },
          "staticResult": {
            "type": [ "string", "null" ],
            "default": null
          },
          "scriptBody": {
            "type": [ "string", "null" ],
            "default": null
          },
          "enabled": {
            "type": "boolean",
            "default": true
          }
        }
      }
    },
    "scenarioDefault": {
      "type": "string",
      "description": "Optional Vector 8 default scenario text (non-persona). Engine reads scenario via chatMeta.groupScenarioText. Per-chat auto-install deferred — tonight the bundle SHIPS the string; next session's installer can PATCH it onto chat metadata."
    },
    "lorebook": {
      "type": "object",
      "description": "Lorebook + entries auto-imported into Marinara's lorebook database.",
      "required": [
        "name",
        "entries"
      ],
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1,
          "maxLength": 200
        },
        "description": {
          "type": "string"
        },
        "category": {
          "type": "string",
          "enum": [
            "world",
            "character",
            "npc",
            "spellbook",
            "uncategorized"
          ],
          "default": "world"
        },
        "scanDepth": {
          "type": "integer",
          "minimum": 0,
          "default": 4
        },
        "tokenBudget": {
          "type": "integer",
          "minimum": 0,
          "default": 1500
        },
        "recursiveScanning": {
          "type": "boolean",
          "default": false
        },
        "entries": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "name",
              "content"
            ],
            "properties": {
              "name": {
                "type": "string",
                "minLength": 1,
                "maxLength": 200
              },
              "content": {
                "type": "string"
              },
              "keys": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "secondaryKeys": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "position": {
                "type": "integer",
                "minimum": 0,
                "maximum": 2,
                "default": 0,
                "description": "0 = before character defs (system context), 1 = after, 2 = depth-injected (uses depth field)."
              },
              "depth": {
                "type": "integer",
                "minimum": 0,
                "default": 4
              },
              "selective": {
                "type": "boolean",
                "default": false
              },
              "constant": {
                "type": "boolean",
                "default": false
              },
              "role": {
                "type": "string",
                "enum": [
                  "system",
                  "user",
                  "assistant"
                ],
                "default": "system"
              }
            }
          }
        }
      }
    }
  }
}
